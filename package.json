{
  "name": "text-editor",
  "version": "1.0.0",
  "description": "A text-editor application",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "rubaba",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.3"
  },
  "dependencies": {
    "express": "^4.18.2",
    "if-env": "^1.0.4"
  }
}
